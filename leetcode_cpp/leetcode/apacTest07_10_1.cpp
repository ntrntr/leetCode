#include <vector>
#include <iostream>
#include <string.h>
#include <algorithm>
#include <string>
#include <map>
#include <stack>
#include <unordered_map>
#include <deque>
#include <limits.h>
#include <fstream>
using namespace std;
int GetDifferentLen(const string& a)
{

}
bool cmp(const string& a, const string& b)
{

}
int main()
{
	freopen("D:\\ÓÃ»§Ä¿Â¼\\Downloads\\A-large-practice.in", "r", stdin);
	ofstream os("D:\\res.txt");
	int t, n;
	cin >> t;
	while (t--)
	{
		cin >> n;
		char s[21];
		vector<string> data(n);
		int count = 0;
		while (n--)
		{
			getline(cin, data[count++]);
		}
		sort(data.begin(), data.end(), []()
		{

		});
	}
	
	return 0;
}